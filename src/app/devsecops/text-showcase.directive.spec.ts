import { TextShowcaseDirective } from './text-showcase.directive';

describe('TextShowcaseDirective', () => {
  it('should create an instance', () => {
    const directive = new TextShowcaseDirective();
    expect(directive).toBeTruthy();
  });
});
