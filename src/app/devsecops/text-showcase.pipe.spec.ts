import { TextShowcasePipe } from './text-showcase.pipe';

describe('TextShowcasePipe', () => {
  it('create an instance', () => {
    const pipe = new TextShowcasePipe();
    expect(pipe).toBeTruthy();
  });
});
