import { SearchBusinesssPipe } from './search-businesss.pipe';

describe('SearchBusinesssPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBusinesssPipe();
    expect(pipe).toBeTruthy();
  });
});
